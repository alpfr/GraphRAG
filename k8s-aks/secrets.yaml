apiVersion: v1
kind: Secret
metadata:
  name: graphrag-secrets
  labels:
    app: graphrag
type: Opaque
stringData:
  # Base64 encoding is handled automatically by stringData in K8s
  OPENSEARCH_PASSWORD: "Gr@phR@g!2026_OpenSearch!"
  NEO4J_PASSWORD: "password123"
  
  # API Keys for Cloud LLMs 
  # Replace these with real keys before applying to the cluster!
  OPENAI_API_KEY: "your-openai-api-key"
  ANTHROPIC_API_KEY: "your-anthropic-api-key"
  GOOGLE_API_KEY: "your-google-api-key"
